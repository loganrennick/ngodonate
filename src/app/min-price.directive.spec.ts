import { MinPriceDirective } from './min-price.directive';

describe('MinPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new MinPriceDirective();
    expect(directive).toBeTruthy();
  });
});
